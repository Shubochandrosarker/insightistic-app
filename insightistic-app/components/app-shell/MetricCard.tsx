// Canonical metric card for the app shell. Re-exports the dashboard KPI card so
// admin + dashboard share one implementation (label, value, delta, sparkline).
export { KpiCard as MetricCard } from "@/components/dashboard/KpiCard";
